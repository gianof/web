<% layout('layout') -%>

<div class="container">
  <div class="row">
    <div class="col-sm-6 col-md-4">
      <img class="img-circle img-responsive img-center" src="<%= user.profile.picture %>" alt="">

      <br />
      <p>Όνομα: <%= user.profile.name %></p>
      <p>Email: <%= user.email %></p>

      <% if (user.address === "" || typeof user.address === 'undefined')  { %>
        <p>Διεύθυνση: <span style="color: #c0392b; font-weight: bold">Δεν έχετε διεύθυνση</span></p>

      <% } else { %>

      <p>Διεύθυνση: <%= user.address %></p>

      <% } %>

      <a href="/edit-profile" class="btn btn-primary btn-lg">Επεξεργασία Προφίλ</a>
    </div>

    <div class="col-md-6">
      <h2 class="text-center">Ιστορικό</h2>

      <table class="table table-striped sticky-header">
        <thead>
        <tr>
          <th>Προϊόν</th>
          <th>Τιμή: </th>
        </tr>
        <tbody>
          <% for(var i = 0; i < user.history.length; i++) {%>
            <tr>
              <td><%= user.history[i].item.name %></td>
              <td><%= user.history[i].paid %></td>
            </tr>
          <% } %>
        </tbody>
        </thead>
      </table>

    </div>
  </div>
</div>
